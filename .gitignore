# Rust build output (workspace root, and the workspace-excluded vendored crate, which
# builds into its own target/ since it isn't a member).
/target
vendor/mp4/target/

# Editor / OS cruft
*.rs.bk
*.swp
.DS_Store

# Local Claude Code working notes / progress tracking (not shared)
.claude/
