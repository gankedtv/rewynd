import { defineConfig } from 'astro/config';

// Static marketing site for rewynd. Update `site` once the real domain is chosen —
// it drives canonical + Open Graph URLs.
export default defineConfig({
  site: 'https://rewynd.gg',
});
